<UserControl xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"  x:Class="Dentists.View.Patient.AllPatientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:ViewModels="clr-namespace:Dentists.View.Patient"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <ViewModels:AllPatientViewModel/>
    </UserControl.DataContext>
    <Grid>
        <dxg:GridControl ItemsSource="{Binding Path=AllPatients}">
            <dxg:GridControl.View>
                <dxg:CardView>
                    <dxg:CardView.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock>ages</TextBlock>
                                <TextBox Text="{Binding Path=Age}"></TextBox>
                            </Grid>
                        </DataTemplate>
                    </dxg:CardView.CellTemplate>
                </dxg:CardView>
            </dxg:GridControl.View>
        </dxg:GridControl>
    </Grid>
</UserControl>
